# CRF 微信小程序部署指南

## ✅ 已完成的准备工作

### 1. 项目配置
- ✅ AppID 已配置：`wx51fce73370eaf709`
- ✅ 项目路径：`C:\Users\Administrator\WorkBuddy\2026-05-13-task-1\crf-miniprogram\`
- ✅ 后端 API：`https://zhongyibianzhengdafen.fun:8000/CRF`

### 2. 依赖安装
- ✅ `tdesign-miniprogram` 已安装到 `miniprogram/node_modules/`

### 3. npm 构建（手动完成）
- ✅ 已将 `tdesign-miniprogram/miniprogram_dist/*` 复制到 `miniprogram/miniprogram_npm/tdesign-miniprogram/`
- ✅ `miniprogram_npm/tdesign-miniprogram/` 目录已创建，包含所有 TDesign 组件

### 4. 项目配置更新
- ✅ `project.config.json` 已添加 `useNpmModules: true`
- ✅ `packNpmManually: true` 和 `packNpmRelationList` 已配置

---

## 📋 你需要在微信开发者工具中完成的步骤

### 步骤1：导入项目
1. 打开微信开发者工具
2. 点击「导入项目」
3. 选择项目目录：`C:\Users\Administrator\WorkBuddy\2026-05-13-task-1\crf-miniprogram`
4. 点击「导入」

**验证**：项目成功打开，不报错。

---

### 步骤2：构建 npm
1. 在微信开发者工具顶部菜单，点击「工具」
2. 选择「构建 npm」
3. 等待构建完成（可能需要几分钟）

**说明**：这一步会重新生成 `miniprogram_npm/` 目录中的 TDesign 组件产物。虽然我们手动复制了，但微信开发者工具的构建过程会做一些额外的处理（如：将 ES6 代码转换为 ES5，处理小程序特定的文件等）。

**验证**：构建成功，无错误提示。

---

### 步骤3：详情设置
1. 在微信开发者工具右上角，点击「详情」
2. 在「本地设置」标签页中，勾选以下选项：
   - ✅ 「使用 npm 模块」
   - ✅ 「使用 ES6 转 ES5」（如果没勾选）
   - ✅ 「增强编译」（如果没勾选）
3. 确保「不校验合法域名」已勾选（开发阶段）

**验证**：设置已保存，项目可正常编译。

---

### 步骤4：编译预览 → 测试登录流程
1. 在微信开发者工具顶部，点击「编译」按钮
2. 模拟器应正常加载「登录页」
3. 测试登录流程：
   - 输入用户名和密码
   - 点击登录
   - 验证是否能成功跳转至首页

**验证**：
- 登录页 UI 正常显示（TDesign 组件正常渲染）
- 登录 API 调用成功（检查「调试器」→「Network」面板）
- 成功跳转至首页

---

### 步骤5：测试没有问题自动发布
**前提条件**：
1. 微信开发者工具已登录微信账号
2. 该账号有此小程序的「上传权限」
3. （可选）有 `private.key` 文件可以用 `miniprogram-ci` 自动化

**手动发布步骤**：
1. 在微信开发者工具顶部，点击「上传」
2. 填写版本号和项目备注
3. 点击「上传」
4. 上传成功后，登录「微信公众平台」→「开发管理」→「开发版本」，提交审核

**自动化发布（需要私钥文件）**：
如果你有 `private.key` 文件，可以将其放到项目根目录，然后我可以用 `miniprogram-ci` 自动化完成上传。

---

## 🔧 故障排查

### 问题1：TDesign 组件不显示 / 报错
**解决方案**：
1. 确认 `miniprogram/miniprogram_npm/tdesign-miniprogram/` 目录存在
2. 在微信开发者工具中，删除 `miniprogram_npm/` 目录
3. 重新执行「工具」→「构建 npm」

### 问题2：登录 API 调用失败
**解决方案**：
1. 确认后端已部署：`https://zhongyibianzhengdafen.fun:8000/CRF`
2. 在微信公众平台，已将 `zhongyibianzhengdafen.fun:8000` 添加到「服务器域名」
3. 开发阶段可勾选「不校验合法域名」

### 问题3：无法上传代码
**解决方案**：
1. 确认微信开发者工具已登录有上传权限的微信账号
2. 或者，下载「上传密钥」（`private.key`），放到项目根目录
3. 使用 `miniprogram-ci` 自动化上传（我可以帮你写脚本）

---

## 📞 需要帮助？

如果你在安装微信开发者工具或执行步骤时遇到问题，请告诉我：
1. 当前在哪一步？
2. 具体的错误信息是什么？

我可以为你：
- 提供详细的操作截图说明
- 准备 `miniprogram-ci` 自动化脚本（需要 `private.key`）
- 检查项目配置，确保没有遗漏

---

**项目状态**：✅ 已准备好，可以导入微信开发者工具开始测试！